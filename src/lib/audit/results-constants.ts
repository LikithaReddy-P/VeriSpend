/** Show expert CTA when recoverable spend exceeds this threshold (USD / month). */
export const HIGH_SAVINGS_THRESHOLD_USD = 500;

/** Below this monthly total, treat the stack as reasonably optimized. */
export const LOW_SAVINGS_THRESHOLD_USD = 50;
